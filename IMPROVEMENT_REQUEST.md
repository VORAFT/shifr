
# Improvements request

## 1. [script.js](script.js#1)

Необходимо использовать `const` всегда (забываем про `var`), и `let` только в случае когда переменную необходимо изменить в процессе работы программы (не распространяется на объекты и массивы, там всегда `const`). Необходимо Заменить все объявления переменных на `const`, оставить `let` только там, где переменная действительно переприсваивается


## 2. [script.js](script.js#L10)

Составить правильную иерархию отступов. 

Правильно переименовать `encrypt` вместо `crypt` 😁


## 3. [script.js](script.js#L13-L28)

Стараться использовать методы массива в приоритете перед циклами. В данном случае `reduce`


## 4. [script.js](script.js#L34-L50)

То же самое (п. 3)